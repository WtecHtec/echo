# Echo 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名称 | Echo |
| 文档版本 | v1.0 |
| 平台 | Electron 桌面客户端（Win / Mac / Linux） |
| 数据存储 | 本地 JSON 文件（按文章分文件夹管理） |
| 撰写日期 | 2026-07-23 |

---

## 1. 产品概述

### 1.1 一句话描述
Echo 是一款本地化的英语精听精读训练工具，用户输入或导入英语文章后，借助大模型自动生成句子/单词的音标、翻译，并支持朗读与默写训练，帮助用户通过"听 - 看 - 写"的方式深度掌握文章内容与词汇。

### 1.2 背景与动机
市面上的背单词类 App 大多基于固定词库，与用户实际感兴趣的内容脱节，记忆效果有限。Echo 的核心差异化在于：**学习素材来自用户自己导入的文章**，天然具备兴趣驱动和语境记忆的优势，同时通过默写这种主动回忆（Active Recall）方式强化记忆效果，比单纯的选择题/填空题类背词工具更扎实。

### 1.3 目标用户
- 有一定英语基础、希望通过精读文章提升听力和拼写能力的学习者
- 需要背诵、默写课文/演讲稿/文章片段的用户（如考试备考、教师布置的任务）
- 希望积累个人生词本、通过原文语境记单词的自学者

### 1.4 核心价值主张
1. **内容自定义**：任何英语文章都能变成学习材料
2. **本地优先**：数据完全存储在本地 JSON，无云端依赖，隐私可控，可自由备份/迁移
3. **主动回忆训练**：默写而非选择，记忆效果更扎实
4. **双模式覆盖**：文章整体理解 + 单词精细记忆，两个颗粒度互补

---

## 2. 产品目标

| 目标类型 | 说明 |
|---|---|
| 用户目标 | 能够快速将一篇英语文章转化为可听、可查、可默写的学习材料 |
| 产品目标 | 打造一个轻量、稳定、离线可用的桌面精读工具 MVP |
| 商业目标（可选） | 暂不考虑商业化，作为个人/小范围工具起步，后续可评估开源或付费高级功能 |

---

## 3. 用户使用流程总览

```
导入文章(粘贴/txt) → LLM解析生成结构化数据(句子+单词) → 查看/朗读结果
        ↓
  ┌─────────────┬─────────────┐
  文章默写模式         单词默写模式
  (整句默写)          (单词默写，含SRS复习)
        ↓                    ↓
   默写结果比对          默写结果比对+SRS更新
        ↓                    ↓
      进度与统计数据保存(progress.json)
```

---

## 4. 功能需求

### 4.1 文章导入与解析（核心功能）

**功能描述：**
- 支持两种导入方式：
  1. 直接粘贴英语文本到输入框
  2. 导入本地 `.txt` 文件
- 导入后系统自动完成：
  1. **分句**：按句子边界拆分全文（需处理缩写、省略号等特殊标点，避免误切）
  2. **分词与去重**：提取文章中出现的单词，去除重复项，生成词表
  3. **调用大模型生成结构化内容**，包括：
     - 每句话的中文翻译
     - 每句话的音标（IPA）标注（整句连读音标，用于跟读参考）
     - 每个单词的音标（IPA）
     - 每个单词的中文释义（结合当前语境的释义，非词典泛义）
     - （可选）单词难度分级（如 CEFR A1-C2）

**关键设计点：**
- 单词表（含音标、释义）直接由 LLM 生成，无需接入本地发音词典或额外的 G2P 引擎，简化技术链路
- LLM 返回结果需为结构化 JSON，按句子/单词分别落库，减少解析出错概率
- 解析结果需支持本地缓存，同一篇文章不重复调用大模型，避免浪费 Token/时间

**验收标准：**
- [ ] 用户粘贴或导入文本后，10 秒内（视文章长度和网络情况）完成解析并展示结果
- [ ] 解析失败（如 LLM 返回格式错误）时有明确的重试/报错提示，不导致数据丢失
- [ ] 生成的文章自动创建独立文件夹并保存

---

### 4.2 文章结果查看（学习模式）

**功能描述：**
- 以句子为单位，逐句展示：
  - 英文原句
  - 句子音标
  - 中文翻译
  - 朗读按钮（点击可听整句发音）
- 单词部分：
  - 列表展示文章中出现的所有单词
  - 每个单词展示音标、释义
  - 朗读按钮（点击可听单词发音）
- 朗读支持**语速调节**（如 0.5x - 1.5x 可调），供用户按需调整

**验收标准：**
- [ ] 每句/每词均可独立点击朗读，音频响应延迟 < 500ms
- [ ] 语速调节实时生效，无需重新进入页面
- [ ] 音标/翻译展示清晰，支持字体缩放（面向不同屏幕/视力需求）

---

### 4.3 文章默写模式

**功能描述：**
- 以整句为单位进行默写训练，流程：
  1. 系统展示当前句子的音标（不展示原文/翻译）
  2. 用户在输入框中默写出完整英文句子
  3. 用户提交后，系统进行文本比对，高亮显示错误的单词/拼写差异
  4. 用户可选择性操作：
     - 查看该句音标（若之前隐藏）
     - 查看该句翻译（辅助理解句意）
     - 点击朗读该句（辅助拼写回忆）
  5. 完成后进入下一句，直至全文默写完毕
- 默写过程中若使用了"查看翻译/朗读"等辅助操作，系统需记录该次默写"使用了提示"，用于后续统计真实掌握度

**验收标准：**
- [ ] 默写比对能准确定位到具体错误单词/拼写差异（而非仅判断整句对错）
- [ ] 支持逐句前进/后退，用户可自由切换默写顺序
- [ ] 默写完成后展示本次训练总结（正确率、耗时、使用提示次数等）

---

### 4.4 单词默写模式

**功能描述：**
- 支持两种练习范围选择：
  - 仅练习当前文章的单词
  - 练习全局生词本（跨文章汇总的单词）
- 默写流程：
  1. 系统随机播放一个单词的发音（音频朗读）
  2. 用户根据发音默写出该单词的拼写
  3. 提交后系统判断对错，展示正确拼写
  4. 用户可选操作：
     - 查看音标
     - 查看释义
     - 重新朗读（可调语速）
     - 下一个单词（跳过）
- 单词练习需接入 **间隔重复算法（SRS，如 SM-2 模型）**：
  - 根据用户历史默写正确率、上次复习时间，动态调整单词出现频率
  - 容易出错的单词优先/更频繁出现，已掌握的单词逐渐减少出现频率

**验收标准：**
- [ ] 单词随机播放逻辑接入 SRS 算法后，实际验证"出错单词复现频率高于已掌握单词"
- [ ] 默写正确/错误后，SRS 状态实时更新并持久化保存
- [ ] 支持标记单词为"已掌握"，后续默认从常规轮次中排除（除非用户手动重置）

---

### 4.5 朗读与语音控制

**功能描述：**
- 默认使用系统内置语音合成（如 Electron/Chromium 自带 Web Speech API），实现离线免费朗读
- 支持语速调节（滑块或预设档位：0.5x / 0.75x / 1x / 1.25x / 1.5x）
- （可选/后续版本）支持接入云端高质量 TTS（如 Azure/Google Neural TTS）作为"高清朗读模式"，需用户自行配置 API Key

**验收标准：**
- [ ] 语速调节全局生效，句子/单词朗读均遵循同一语速设置
- [ ] 离线状态下朗读功能正常可用

---

### 4.6 数据管理与文件结构

**功能描述：**
- 每篇导入的文章生成一个独立文件夹，命名规则建议：`{文章ID}-{标题slug}`
- 文件夹内包含：
  - `meta.json`：文章标题、导入时间、句子总数、单词总数、整体学习进度概览
  - `sentences.json`：句子数据数组（原文、音标、翻译）
  - `words.json`：单词数据数组（单词、音标、释义、难度等）
  - `progress.json`：默写进度与结果记录（句子/单词维度，含 SRS 状态、是否使用提示等）
- 全局数据：
  - `/vocabulary/index.json`：跨文章汇总的全局单词库及其 SRS 状态，用于"全局生词本"默写模式
  - `settings.json`：全局设置（语速、语音引擎、大模型 API 配置等，详见 4.7）

**验收标准：**
- [ ] 用户可在系统文件管理器中直接找到并查看/备份对应文章文件夹
- [ ] 删除某篇文章时，同步清理其对全局词库的引用（不误删其他文章仍在使用的单词数据）
- [ ] 数据写入具备基本的异常保护（如写入失败时不损坏原文件，可考虑先写临时文件再替换）

---

### 4.7 设置面板

**功能描述：**
- 提供独立的"设置"页面/面板，供用户配置大模型 API 相关信息，包括：
  - **API URL**：自定义大模型服务地址（支持接入 OpenAI 兼容接口、Anthropic、其他第三方/自建服务）
  - **API Key**：用户自行填写，本地明文或简单加密存储于 `settings.json`，不上传任何服务器
  - **Model**：可填写/选择具体调用的模型名称（如 `gpt-4o`、`claude-sonnet-4-6` 等），支持自由输入以兼容未来新模型
- 设置面板需支持：
  - 保存后立即生效，无需重启应用
  - 提供"测试连接"按钮，验证 API URL/Key/Model 组合是否可用，便于用户排查配置错误
  - 多套配置预设（可选，非 MVP 必须）：允许用户保存多组 API 配置并快速切换（如日常用便宜模型，精细校对时切换更强模型）
- 其他设置项（可与 LLM 配置同页或分 Tab）：
  - 默认朗读语速
  - 默认语音引擎（系统 TTS / 云端 TTS，如后续接入）

**验收标准：**
- [ ] 用户填写 API URL、Key、Model 后可保存并立即用于后续文章解析请求
- [ ] "测试连接"能准确反馈配置是否有效（成功/网络错误/鉴权失败等具体提示，而非笼统报错）
- [ ] 未配置或配置无效时，导入文章功能应有明确提示引导用户先完成设置，而非静默失败
- [ ] API Key 在界面展示时默认隐藏（如密码框形式），避免误泄露

---

## 5. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 单篇 3000 词以内文章的解析时间应控制在可接受范围内（视 LLM 响应速度，建议增加进度提示 UI） |
| 离线能力 | 除首次 LLM 解析需要联网外，其余朗读、默写、查看功能应完全离线可用 |
| 数据安全 | 用户数据全部本地存储，不上传任何服务器；若接入云端 TTS/LLM，需明确告知用户数据会发送至第三方 API |
| 跨平台 | 需保证 Windows / macOS / Linux 三端基本功能一致 |
| 可移植性 | 用户可整体拷贝数据目录到其他设备继续使用 |

---

## 6. 技术方案要点（供研发参考）

1. **音标与单词表生成策略**：单词的音标、释义均直接由 LLM 生成，不再引入本地发音词典或 G2P 引擎，降低技术复杂度；需通过 Prompt 设计尽量约束输出格式规范，减少音标错误率
2. **LLM 调用**：大模型负责翻译、音标标注、语境化释义、难度分级等任务，返回严格的结构化 JSON，减少解析出错；API URL/Key/Model 均从设置面板读取（见 4.7），支持用户自由切换服务商与模型
3. **朗读引擎**：Electron 基于 Chromium，可直接使用浏览器原生 `speechSynthesis` API 实现离线朗读与语速控制，无需额外服务
4. **数据存储**：采用纯 JSON 文件读写（Node.js `fs` 模块），避免引入 native 模块（如 better-sqlite3）带来的跨平台编译负担，匹配当前数据量级（单用户、非海量数据）的实际需求
5. **SRS 算法**：建议采用经典 SM-2 算法或其简化变种，作为单词默写模式的排序逻辑核心

---

## 7. 里程碑规划（建议）

| 阶段 | 内容 |
|---|---|
| MVP v0.1 | 文章导入解析、结果查看（句子+单词朗读/音标/翻译）、文章默写模式 |
| v0.2 | 单词默写模式（不含 SRS，先随机播放） |
| v0.3 | 接入 SRS 算法，支持全局生词本、单词掌握状态管理 |
| v0.4 | 设置页完善（语速/语音引擎/API Key 管理）、数据导出（如导出 Anki 格式） |
| v1.0 | 整体体验打磨、异常处理完善、跨平台打包发布 |

---

## 8. 风险与待定事项

- **音标准确性**：完全依赖 LLM 生成音标存在一定幻觉风险（尤其生僻词、多音词、专有名词），需通过 Prompt 优化尽量降低错误率，并考虑提供用户手动纠错入口
- **LLM 成本与稳定性**：需考虑用户自备 API Key 还是产品内置额度，以及大模型返回格式不稳定时的容错机制
- **默写比对算法**：整句默写的比对逻辑需处理大小写、标点、同义替换等边界情况，避免"实际正确但被判错"的体验问题
- **待确认问题**：
  - 是否需要支持多语言界面（当前默认中文界面 + 英语学习内容）？
  - 是否需要支持用户自定义单词释义/音标（人工纠错 LLM 生成结果）？

---

*本文档为初版 PRD，后续可根据开发进展与用户反馈迭代更新。*